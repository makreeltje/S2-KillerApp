@{
    ViewData["Title"] = "Movies";
}
@model IEnumerable<PMDB_docker.Models.Movie>

<div class="card-deck">
    @foreach (var movie in Model)
    {
        <div class="card m-3">
            <div class="card-header">
                <h5>@movie.Title</h5>
                <h7>@movie.Genre</h7>
            </div>
            <img class="card-img-top" src="~/img/noimage.png" asp-append-version="true"/>
            <div class="card-footer text-center">
                <a asp-controller="Movie" asp-action="Details" asp-route-id="@movie.Id" class="btn btn-primary">View</a>
                <a href="#" class="btn btn-primary">Edit</a>
                <a href="#" class="btn btn-danger">Delete</a>
            </div>
        </div>
    }
</div>